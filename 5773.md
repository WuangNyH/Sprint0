# 🧪 Checklist Kiểm Thử Item Common

Mục tiêu: đảm bảo **đồng bộ dữ liệu**, **đúng behavior**, **đồng nhất giao diện**, **ổn định hiệu năng**, **an toàn bảo mật**.

---

## 1. 🔄 Đồng Bộ Dữ Liệu
- [ ] Dữ liệu trên UI trùng khớp với dữ liệu từ API/DB.
- [ ] CRUD (Create/Read/Update/Delete) cập nhật ngay, không trễ/lỗi.
- [ ] Refresh/truy cập lại trang vẫn giữ trạng thái đúng.
- [ ] Dữ liệu đồng bộ khi mở ở nhiều tab/browser cùng lúc.
- [ ] Cache/LocalStorage cập nhật đúng sau khi thay đổi dữ liệu.
- [ ] Đồng bộ timezone, ngày/giờ hiển thị theo chuẩn hệ thống.

---

## 2. ⚙️ Behavior & Luồng Chức Năng
- [ ] Nút bấm (Save, Cancel, Delete, Back) hoạt động đúng hành vi mong đợi.
- [ ] Input validation hoạt động chuẩn (required, email, số điện thoại, date range).
- [ ] Trả về thông báo lỗi rõ ràng khi nhập sai dữ liệu.
- [ ] Thông báo thành công/thất bại (toast, popup, alert) hiển thị đúng ngữ cảnh.
- [ ] Search/filter hoạt động chính xác (keyword, khoảng giá trị, multi filter).
- [ ] Sort đúng cột/trường, giữ nguyên thứ tự khi reload.
- [ ] Pagination / Load more hiển thị đúng số lượng item và chuyển trang mượt mà.
- [ ] Upload/download file thành công, đúng định dạng, đúng dung lượng cho phép.
- [ ] Multi-step form: chuyển bước đúng logic, back/next giữ dữ liệu.
- [ ] Undo/redo (nếu có) hoạt động chuẩn.

---

## 3. 🎨 UI/UX & Trải Nghiệm Người Dùng
- [ ] Layout chuẩn với design system (khoảng cách, căn lề, font, màu).
- [ ] Responsive trên desktop, tablet, mobile (không vỡ layout).
- [ ] Icon, màu sắc, font đồng bộ theo guideline.
- [ ] Loading state hiển thị khi API chưa trả về.
- [ ] Empty state rõ ràng, có icon/placeholder hợp lý.
- [ ] Error state hiển thị thông tin hữu ích, không crash.
- [ ] Hover/active/focus state của button, input hoạt động đúng.
- [ ] Accessibility: có aria-label, tab navigation, contrast màu đạt chuẩn.
- [ ] Dark mode (nếu có) hiển thị đúng.

---

## 4. 🔒 Bảo Mật
- [ ] Chặn truy cập vào màn hình/API khi chưa login.
- [ ] Phân quyền (Role/Permission) hoạt động chuẩn: user không thể truy cập tính năng của admin.
- [ ] Session/token hết hạn sẽ logout đúng cách.
- [ ] Không hiển thị dữ liệu nhạy cảm trong log/console.
- [ ] Input được sanitize để tránh XSS/SQL Injection.
- [ ] File upload kiểm soát loại file, kích thước, tránh upload script.
- [ ] Mật khẩu & dữ liệu nhạy cảm được mã hóa khi lưu & truyền.

---

## 5. 🚀 Hiệu Năng & Ổn Định
- [ ] API response < 3 giây trong điều kiện bình thường.
- [ ] UI load nhanh, không block thread chính.
- [ ] Hệ thống hoạt động ổn khi nhiều user thao tác đồng thời.
- [ ] Không bị memory leak sau khi thao tác lâu dài.
- [ ] Retry logic (nếu có) hoạt động đúng khi API fail.
- [ ] Offline mode (nếu có) hoạt động chuẩn, sync dữ liệu khi online lại.

---

## 6. 🌐 Compatibility
- [ ] Hoạt động trên các trình duyệt phổ biến (Chrome, Edge, Firefox, Safari).
- [ ] Hoạt động trên hệ điều hành khác nhau (Windows, MacOS, Linux).
- [ ] Mobile test trên Android/iOS, kiểm tra trên các phiên bản phổ biến.
- [ ] Font hiển thị đúng trên các môi trường.
- [ ] Ngôn ngữ (đa ngôn ngữ nếu có) hiển thị đúng ký tự, không lỗi font.

---

## 7. 📊 Logging & Monitoring
- [ ] Log ghi nhận đủ thông tin khi có lỗi.
- [ ] Không log tràn console gây ảnh hưởng performance.
- [ ] Monitoring (nếu có) bắt được lỗi 500, timeout, network error.
- [ ] Notification (email/slack/discord) gửi đúng khi có sự cố.

---

## 8. 🧩 Common Component Test (Form, Modal, Table, Toast…)
- [ ] Modal mở/đóng đúng behavior, overlay che đúng màn hình.
- [ ] Form validation hoạt động đồng bộ (cả client & server).
- [ ] Table: có scroll khi quá nhiều cột/hàng.
- [ ] Toast/notification: auto close đúng thời gian, có nút close.
- [ ] Dropdown, select box hiển thị đủ giá trị, chọn đúng.
- [ ] Checkbox, radio button lưu đúng state.
- [ ] Datepicker chọn ngày hợp lệ, chặn ngày không cho phép.

---

## 9. 📦 Quy trình Release & Deploy (liên quan common test)
- [ ] Build/test pipeline chạy thành công trước khi deploy.
- [ ] Artifact (jar, war, bundle, docker image) tạo ra đúng version.
- [ ] Môi trường staging = production (tối thiểu 90%).
- [ ] Test smoke sau deploy: login, load trang chính, API chính.
- [ ] Rollback strategy sẵn sàng nếu deploy lỗi.

