# 📑 Tài liệu hóa & Lưu trữ File Template Lỗi

## 1. Mục đích

* Đảm bảo **một nguồn duy nhất** để quản lý lỗi và template.
* Tạo sự **thống nhất** về cách ghi nhận, lưu trữ và sử dụng lỗi trong dự án.
* Thuận tiện cho việc **review, phân tích và học hỏi từ lỗi** trong buổi Retro.

---

## 2. Vị trí lưu trữ

* **Mục Bug trong Techkan**

---

## 3. Quy trình quản lý Template

1. **Tải file lỗi cho Sprint trong Techkan**

   * Truy cập kho chung → mở `Error_Log_Template`.
   * Tạo bản copy → đổi tên thành:

     ```
     Error_Log_[ProjectName]_SprintXX
     ```

     Ví dụ: `Error_Log_EcomApp_Sprint12`.

2. **Ghi nhận lỗi**

   * Dùng đúng format trong template (các cột: ID, Ngày phát hiện, Người phát hiện, Mô tả lỗi, Trạng thái...).
   * Bắt buộc điền đầy đủ **các trường tối thiểu**


3. **Cập nhật tiến độ**

   * Dev Lead assign lỗi cho người xử lý.
   * Dev cập nhật trạng thái khi fix.
   * QA verify & đóng lỗi.

4. **Sử dụng trong Retro**

   * Scrum Master xuất thống kê lỗi từ file sprint.
   * Team cùng phân tích nguyên nhân, học từ lỗi.
   * Ghi lại action cải tiến trực tiếp trong file.

